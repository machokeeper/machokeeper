---
name: Bug report
about: A binary that should be fixed isn't, or a repair went wrong
labels: bug
---

**What happened**

**`machokeeper doctor --json <path>` output**

```
```

**Platform**: (macOS version / NixOS / nix-darwin / home-manager)
**machokeeper version**: (`machokeeper version`)
**Is the path a GC root / login shell?** (needs `--fix-live`)
